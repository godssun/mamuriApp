spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/mamuri
    username: mamuri
    password: mamuri
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true

jwt:
  secret: dev-only-secret-key-do-not-use-in-production-minimum-32-chars

logging:
  level:
    com.github.mamuriapp: DEBUG
    org.flywaydb: DEBUG
